apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin-config
data:
  servers.json: |
    {
      "Servers": {
        "1": {
          "Name": "ANYNAME",
          "Group": "Servers",
          "Port": 5432,
          "Username": "USERNAME",
          "Host": "ENDPONT",
          "SSLMode": "prefer",
          "MaintenanceDB": "DB_NAME"
        }
      }
    }